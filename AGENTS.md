# 编码 agent 全局规则：开发与修复过程中的 agent 调用与额度控制

适用于任何编码 agent 终端。正文只用通用说法；模型档位、命令名等厂商专有内容见文末的术语映射（安装时附上）。

目标是完成任务，额度（token）是要压缩的成本。上下文里的每个 token 在之后每一轮都会再付一次费，
输出 token 单价又是输入的数倍。所以所有规则都指向：少往上下文放东西、少走弯路、少重复劳动、少说废话。

## 总原则
1. 先想清楚再动手：一次澄清或几行计划，远比一次返工便宜。
2. 只把必要内容放进上下文：定位用搜索，读文件只读需要的行段，命令输出一律裁剪。
3. 证据驱动，不猜测试错：同一问题连续两次修复无效就停下补证据，不再试变体。
4. 派子 agent 是为了把大体量的中间输出隔离在主上下文之外，不是为了"显得全面"。

## 一、何时派子 agent
派之前回答三个问题，任一为"否"就自己做：
- 中间输出（跨多文件的搜索、测试或日志全文、网页内容）是否大到会污染主上下文？
- 任务能否用一段自包含的提示词说清（目标、范围、已知结论、返回格式）？
- 结果是否是主线必需的，而不是"顺便看看"？

默认不派：
- 已知文件、符号或值的单一事实查询：直接搜索、直接读。
- 报错或栈追踪已指明文件和行：直接读那一段。
- 改动不超过 3 个文件：主会话自己做。
- 多 agent 编排（工作流、agent 团队、扇出再汇总）与"多维度评审"（bug、性能、风格、安全各派一个）：普通开发与修复任务不用。
- 当前终端没有子 agent 能力时，第一、二节不适用，其余规则照常。

评审只在两种情况下派一次评审员：改动超过 3 个文件，或触及输入处理、鉴权、并发、文件/网络/子进程调用。
评审员查正确性与安全缺陷（注入、越权、敏感信息泄漏），不查风格与性能。其余改动不派评审。

## 二、派子 agent 的规范
- 提示词自包含：目标、涉及文件路径、已知结论（不让它重新发现）、完成标准、返回格式与上限
  （例如"只返回 文件:行号 和一句结论，不超过 20 行，不贴源码"）。
- 模型按任务分档：搜索定位 → 小模型；跑测试并归纳失败、评审 → 中档模型；
  设计决策、疑难调试 → 主模型。不指定时子 agent 通常继承主模型，那是最贵的选择。各档对应的模型见术语映射。
- 同时最多 3 个子 agent。并行只用于彼此独立且都必需的工作；并行省时间，不省额度。
- 派出后不自己再做同样的搜索，等结果。
- 只有子任务确实需要完整对话历史时才让它继承上下文；其余用自包含提示词的独立子 agent。
- 输出大或耗时长的子 agent 放后台，主会话继续做不依赖它的事。
- 子 agent 报告回来后不要它重发原文；需要细节自己定点读。

## 三、主会话的工具使用
- 定位先搜索（限制条数），再读指定行段；不整个读大文件，不为"了解一下"通读目录。
- 刚编辑过的文件不重读；只有编辑失败才重读。
- git 先看 `--stat` 或 `--name-only`，再看具体文件的 diff。
- 测试、构建、日志输出只看失败与摘要：优先用 run-quiet 脚本包装命令，否则用 `tail`、`grep -E "FAIL|ERROR"`；完整输出落到文件，需要时定点看。
- 本地找不到答案才查网页；抓取网页必须带明确的提取目标，不拉整页。
- 格式化、lint 修复、批量重命名、脚手架生成交给命令，不由模型逐行改写。
- 回复只讲结论与必要依据，不复述已做的操作，不贴已写入文件的代码。
- 脱敏：密钥、令牌、账号、个人信息、内网地址不写进子 agent 提示词、提交信息、PR 与回复；日志和配置先脱敏再放进上下文。

## 四、开发流程（新功能或改动）
1. 澄清：需求有歧义且不同理解会导致不同实现时，一次问清；否则按最合理的解释做并写明假设。随后写 1 到 3 行可执行的验收标准（命令或可观察的结果）。
2. 计划：涉及超过 3 个文件或方案不唯一时，先写不超过 10 行的计划，固定写三样：改哪些文件、要加哪些测试、用什么命令验证；单文件小改不写。
3. 实现：按文件小步推进，写一块验证一块。
4. 验证：中途只跑与改动直接相关的测试或命令；提交前跑一次项目约定的完整检查（lint、类型、测试），输出裁剪。
5. 提交：到里程碑就提交，提交信息说明做了什么。

## 五、修复流程（bug）
1. 复现：用一条最小命令复现并记录失败信息；有测试框架时把复现写成一个失败用例。复现不了先说明，不盲改。
2. 定位：报错或栈追踪 → 搜索 → 读相关行段；不派 agent 泛泛"调查一下"。动手前用一句话写出根因假设和要改的位置。
3. 最小修改：只改导致问题的逻辑，不顺手重构，不格式化无关代码。回归测试与同一根因的其他实例不算扩大范围。
4. 定向验证：先跑复现命令，再跑相关测试；提交前跑一次完整检查。
5. 两次失败即停："一次失败"指一次完整的修改加验证后复现仍失败。连续两次就停止尝试变体：先撤销无效的修改，再取一条新证据（加日志、缩小复现、读上游调用），然后再改。
6. 提交，并在说明里写清根因。

## 六、会话与上下文
- 一个会话只做一件事。任务完成后用户转向无关工作时，提一句可以新开会话（不花额度），不继续堆上下文。
- 长任务在仓库里维护一份简短进度文件：已完成、下一步、验证命令、已试过且无效的修法及其证据、已派出的子 agent 及结论。新会话据此接续，不重新探索。
- 上下文压缩时保留：改动文件清单、未完成项、已试过且无效的修法、已派出的子 agent 及其结论、验证命令。
