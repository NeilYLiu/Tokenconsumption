# 全局规则：开发与修复过程中的 agent 调用与额度控制

目标是完成任务，额度（token）是要压缩的成本。上下文里的每个 token 在之后每一轮都会再付一次费，
输出 token 单价又是输入的数倍。所以所有规则都指向：少往上下文放东西、少走弯路、少重复劳动、少说废话。

## 总原则
1. 先想清楚再动手：一次澄清或几行计划，远比一次返工便宜。
2. 只把必要内容放进上下文：定位用 grep，读文件只读需要的行段，命令输出一律裁剪。
3. 证据驱动，不猜测试错：同一问题连续两次修复无效就停下补证据，不再试变体。
4. 派子 agent 是为了把大体量的中间输出隔离在主上下文之外，不是为了"显得全面"。

## 一、何时派子 agent（Agent 工具）
派之前回答三个问题，任一为"否"就自己做：
- 中间输出（跨多文件的搜索、测试或日志全文、网页内容）是否大到会污染主上下文？
- 任务能否用一段自包含的提示词说清（目标、范围、已知结论、返回格式）？
- 结果是否是主线必需的，而不是"顺便看看"？

默认不派：
- 已知文件、符号或值的单一事实查询：直接 grep / Read。
- 报错或栈追踪已指明文件和行：直接读那一段。
- 改动不超过 3 个文件：主会话自己做。
- "多维度评审"（bug、性能、风格、安全各派一个）：不做。需要评审时只派一个 reviewer，只查正确性。
- Workflow、ultracode、agent teams 这类多 agent 编排：普通开发与修复任务不用。

## 二、派子 agent 的规范
- 提示词自包含：目标、涉及文件路径、已知结论（不让它重新发现）、完成标准、返回格式与上限
  （例如"只返回 file:line 和一句结论，不超过 20 行，不贴源码"）。
- 模型按任务选：搜索定位 → haiku（Explore）；跑测试并归纳失败、只读评审 → sonnet（test-runner、reviewer）；
  设计决策、疑难调试 → 主会话模型。不指定模型时子 agent 继承主会话模型，那是最贵的选择。
- 同时最多 3 个子 agent。并行只用于彼此独立且都必需的工作；并行省时间，不省额度。
- 派出后不自己再做同样的搜索，等结果。
- 需要完整对话上下文的子任务才用 fork；其余用自包含提示词的普通子 agent。
- 输出大或耗时长的子 agent 放后台，主会话继续做不依赖它的事。
- 子 agent 报告回来后不要它重发原文；需要细节自己定点读。

## 三、主会话的工具使用
- 定位先 grep（限制条数），再 Read 指定行段；不 cat 整个大文件，不为"了解一下"通读目录。
- 刚编辑过的文件不重读；只有 Edit 报错才重读。
- git 先看 `--stat` 或 `--name-only`，再看具体文件的 diff。
- 测试、构建、日志输出只看失败与摘要（`tail`、`grep -E "FAIL|ERROR"`），完整输出落到文件，需要时定点看。
- 本地找不到答案才查网页；WebFetch 必须给出明确的提取目标，不拉整页。
- 格式化、lint 修复、批量重命名、脚手架生成交给命令，不由模型逐行改写。
- 回复只讲结论与必要依据，不复述已做的操作，不贴已写入文件的代码。
- 脱敏：密钥、令牌、账号、个人信息、内网地址不写进子 agent 提示词、提交信息、PR 与回复；日志和配置先脱敏再放进上下文。

## 四、开发流程（新功能或改动）
1. 澄清：需求有歧义且不同理解会导致不同实现时，一次问清；否则按最合理的解释做并写明假设。
2. 计划：涉及超过 3 个文件或方案不唯一时，先写不超过 10 行的计划（改哪些文件、如何验证）；单文件小改不写。
3. 实现：按文件小步推进，写一块验证一块。
4. 验证：中途只跑与改动直接相关的测试或命令；提交前跑一次项目约定的完整检查（lint、类型、测试），输出裁剪。
5. 提交：到里程碑就提交，提交信息说明做了什么。

## 五、修复流程（bug）
1. 复现：用一条最小命令复现并记录失败信息；复现不了先说明，不盲改。
2. 定位：报错或栈追踪 → grep → 读相关行段；不派 agent 泛泛"调查一下"。
3. 最小修改：只改导致问题的逻辑，不顺手重构，不格式化无关代码，不扩大范围。
4. 定向验证：先跑复现命令，再跑相关测试；提交前跑一次完整检查。
5. 两次失败即停：同一问题连续两次修复无效，停止尝试变体，先取一条新证据（加日志、缩小复现、读上游调用）再改。
6. 提交，并在说明里写清根因。

## 六、会话与上下文
- 一个会话只做一件事。任务完成后用户转向无关工作时，提一句可以 /clear（免费），不继续堆上下文。
- 长任务在仓库里维护一份简短进度文件（已完成、下一步、验证命令），新会话据此接续，不重新探索。
- 需要压缩时在里程碑处 /compact 并注明保留重点（改动文件清单、未完成项）；compact 本身要读全部历史，不频繁用。
- 长时间空闲后回来，首条请求会重建缓存；先做小事，不一上来就大输出。

## 七、禁止
- 不为"更全面"而派 agent、并行 agent 或做多维度评审。
- 没有新证据不重复运行同一测试或命令。
- 不把整个文件、整份日志、整页网页放进上下文。
- 修 bug 时不顺手重构。
- 本文件保持在 150 行以内，只放每个会话都需要的规则；项目说明放项目 CLAUDE.md，工作流细节放 skill。
